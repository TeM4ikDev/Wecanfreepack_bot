# 🚀 Быстрый старт - Настройка окружений

## Что было сделано

✅ Создана простая система для разных окружений  
✅ Настроены файлы конфигурации для development и production  
✅ Добавлены удобные npm скрипты  
✅ Создан автоматический скрипт переключения окружений  
✅ Перенесены реальные данные конфигурации  

## Как использовать

### 1. Настройка окружения (выберите нужное)
```bash
# Для разработки (тестовый бот)
npm run env:dev

# Для продакшена (основной бот)
npm run env:prod
```

### 2. Запуск приложения
```bash
# Разработка (с автоперезагрузкой)
npm run start:dev

# Продакшен
npm run start:prod
```

### 3. Полный цикл для продакшена
```bash
npm run env:prod
npm run start:prod:build
```

## Файлы конфигурации

- `env.development` - настройки для разработки (тестовый бот)
- `env.production` - настройки для продакшена (основной бот)

## Конфигурация

**Development (тестирование):**
- Бот: `@testscambase`
- Токен: тестовый бот
- API: тестовые ключи

**Production (продакшен):**
- Бот: `@giftthread` 
- Токен: основной бот
- API: продакшен ключи

## Важно!

🔒 **Никогда не коммитьте файлы .env** - они содержат секретные данные  
✅ **Данные уже настроены** - можно сразу использовать  
🚀 **Переключение одной командой** - `npm run env:dev` или `npm run env:prod`  

## Что дальше?

1. Запустите `npm run env:dev` для разработки
2. Запустите `npm run start:dev` для старта приложения
3. Для продакшена: `npm run env:prod` → `npm run start:prod`

Готово! 🎉
